2026-05-21: next-env.d.ts — generated by Next.js (`next dev`/`next build`) AND gitignored (.gitignore:15). Marker added to the working-tree file so future iterations' header-check will skip it; nothing was staged (git refuses ignored files). If Next regenerates the file, the marker is wiped and the next loop iteration will re-mark — benign churn.
2026-05-21: __tests__/*.test.ts — many test files import { describe, it, expect, vi, ... } from "vitest" despite CLAUDE.md Testing Rules §1 ("globals: true ... use without importing"). Repo-wide convention drift; fix in one pass after adding vitest/globals types to tsconfig and lint globals declarations, not piecemeal during this audit.
